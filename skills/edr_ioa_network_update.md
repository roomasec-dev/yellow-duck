# Tool: edr_ioa_network_update

用途：
- 更新 IOA 网络排除规则，属于高危写操作。

风险要求：
- 必须等待用户明确回复"确认"后再执行。
- 执行前要确认网络排除规则 ID 明确无歧义。

输入说明：
- ioc_id：网络排除规则 ID，必填，用于指定要更新的规则。
- plan_name：新的排除名称，可选。
- client_ip：新的 IP 地址，可选。

输出建议：
- 成功后汇报规则 ID 和更新的内容。

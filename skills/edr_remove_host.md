# Tool: edr_remove_host

用途：
- 从管控中移除指定主机，属于高危写操作。移除后将断开与服务器的连接。

风险要求：
- 必须等待用户明确回复"确认"后再执行。
- 执行前要确认主机 client_id 明确无歧义。

输入说明：
- client_id：客户端 ID，必填，多个用英文逗号分隔。

输出建议：
- 成功后汇报已移除的主机数量。
- 同时提醒用户这是高影响动作，移除后主机将断开连接，如果需要可以重新部署客户端。

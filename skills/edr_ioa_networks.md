# Tool: edr_ioa_networks

用途：
- 查询 IOA 网络排除列表，适合回答"有哪些网络排除规则""哪些 IP 被排除"。

输入建议：
- 支持分页查询，page 和 page_size 一起规划。
- 如果用户提到第几页、下一页、每页多少条，要把 page 和 page_size 一起规划出来。

输出建议：
- 优先展示排除规则 ID、名称、IP 地址、主机类型。
- 按最新更新时间优先理解结果。
- 回复里要保留当前页、总页数、是否还有下一页，方便继续翻页。
- 如果结果为空，明确说"当前没有查到 IOA 网络排除规则"。

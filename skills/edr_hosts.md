# Tool: edr_hosts

用途：
- 查询主机基础状态、在线状态、用户、IP、客户端 ID。
- 适合回答“这台机器在不在”“状态怎么样”“这个别名对应哪台主机”。

输入建议：
- 优先从用户话里提取 hostname。
- 如果用户给了 IP，也可以用 client_ip 辅助查询。
- 如果没拿到稳定目标，不要盲查，先让用户补充。

输出建议：
- 先给结论，再列关键字段。
- 如果结果为空，明确说“当前没查到匹配主机”。
- 如果有多台命中，提醒用户继续缩小范围。

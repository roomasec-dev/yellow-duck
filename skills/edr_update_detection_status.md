# Tool: edr_update_detection_status

用途：
- 更新检测记录的状态（标记为已处理、误报等），属于高危写操作。

风险要求：
- 必须等待用户明确回复"确认"后再执行。
- 执行前要确认检测 ID 和处置状态明确无歧义。

输入说明：
- ids：检测 ID 列表，必填。
- deal_status：处置状态（整数），必填。常见值：1 已处理、2 误报、3 威胁、4 待处理等。

输出建议：
- 成功后汇报更新的检测数量。

# Tool: edr_ioa_network_add

用途：
- 添加 IOA 网络排除规则，属于高危写操作。

风险要求：
- 必须等待用户明确回复"确认"后再执行。
- 执行前要确认网络排除的名称、IP 等关键参数明确无歧义。

输入说明：
- plan_name：排除名称，必填。
- client_ip：IP 地址，可选。
- ioc_host_type：主机类型，可选。

输出建议：
- 成功后汇报添加的排除规则名称和添加结果。
- 同时提醒用户这是高影响动作。

# Tool: edr_incident_summary

用途：
- 按 incident_id 查询事件摘要信息（用于快速确认事件背景、主机关联和处置状态）。

输入建议：
- incident_id 必填，优先使用真实工具结果中出现过的值。
- 当用户说“查看这个事件”“相关事件详情”时优先使用本工具。

输出建议：
- 优先展示事件 ID、名称/类型、风险等级、主机、状态、关键时间。
- 如果未查到结果，明确提示 incident_id 可能无效或无权限。
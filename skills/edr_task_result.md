# Tool: edr_task_result

用途：
- 查询指令任务的执行结果，适合回答"某个任务执行了什么""任务返回了什么信息""进程详情"。

输入建议：
- task_id：任务 ID，必填，需要用户提供。
- 用户可能说"查看任务结果 xxx"或"task_id=xxx"。

输出建议：
- 优先展示主机名、指令名称、返回消息。
- 如果有进程信息，展示进程数量、进程详情（PID、名称、路径、哈希、签名）。
- 如果有镜像信息，展示镜像数量、镜像路径、SHA1、是否系统镜像。
- 如果结果为空，明确说"任务结果为空"。
- 不要只是罗列字段，要告诉用户这些结果意味着什么。
